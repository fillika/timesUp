import { createTheme } from '@material-ui/core/styles';

export const datePickerTheme = createTheme({
  palette: {
    primary: {
      main: '#6a5acd',
    },
    secondary: {
      main: '#dc3545',
    },
  },
});