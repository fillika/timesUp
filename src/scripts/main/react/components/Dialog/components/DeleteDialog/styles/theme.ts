import { createTheme } from '@material-ui/core/styles';

export const deleteModalTheme = createTheme({
  palette: {
    primary: {
      main: '#dc3545',
    },
    secondary: {
      main: '#aeaeae',
    },
  },
});