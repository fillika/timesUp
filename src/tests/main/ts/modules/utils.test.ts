describe('Utils test', () => {
  test.todo("Don't forget about utils");

  // test('First test', () => {
  //   const result = Utils.toUpperCase('MyResult');
  //   expect(result).toBe('MYRESULT');
  // });

  // test('Parsing url without query', () => {
  //   const parsedUrl = Utils.parseUrl('http://localhost:8080/login');

  //   expect(parsedUrl.href).toBe('http://localhost:8080/login');
  //   expect(parsedUrl.port).toBe('8080');
  //   expect(parsedUrl.path).toBe('/login');
  //   expect(parsedUrl.query).toEqual({});
  // });

  // test('Parsing url with query', () => {
  //   const parsedUrl = Utils.parseUrl('http://localhost:8080/login?user=user&password=qwerty#two');
  //   const expectedQuery = {
  //     user: 'user',
  //     password: 'qwerty',
  //   };

  //   expect(parsedUrl.hash).toBe('#two');
  //   expect(parsedUrl.query).toEqual(expectedQuery);
  // });
});
